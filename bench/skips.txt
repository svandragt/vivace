# Known per-project bench failures to skip instead of re-running.
# Format: <tool> <version> <project> <scenario>
#   scenario "install" skips the tool for the whole project (cold, warm,
#   no-op and update-warm); "update-warm" skips only that scenario.
# An entry is ignored once the tool reports a different version, so a
# newer release is retried automatically. bench/corpus.sh records each
# skip it applies as a footnote in corpus.md.

riff 0.0.7 yiisoft/yii2-app-basic install
riff 0.0.7 laravel/laravel update-warm
riff 0.0.7 drupal/recommended-project update-warm
riff 0.0.7 roots/bedrock update-warm
riff 0.0.7 phpunit/phpunit update-warm
riff 0.0.7 slimphp/Slim-Skeleton update-warm
riff 0.0.7 statamic/statamic update-warm

# #220: riff's own mirror-mode 304 race (see bench/run.sh's comment above
# its hardcoded mirror-mode skip); these are the corpus entries it hits.
riff 0.0.7 symfony/demo update-warm
riff 0.0.7 composer/composer update-warm
riff 0.0.7 craftcms/craft update-warm
vivacity 0.6.0 roots/bedrock install
vivacity 0.6.0 drupal/recommended-project install
vivacity 0.6.0 yiisoft/yii2-app-basic install
vivacity 0.6.0 craftcms/craft install
