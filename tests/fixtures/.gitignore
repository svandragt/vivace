# Fixture trees may contain a vendor/ directory; the global gitignore
# hides those, which has twice made a test pass locally against files CI
# never had. Repo rules beat the global file, so un-ignore here.
!vendor/
!**/vendor/

# A show test installs into this fixture; the result is scratch, not a golden.
show/outdated-monolog/vendor/
