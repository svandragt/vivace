name: site

on:
  push:
    branches: [main]
    paths:
      - site/**
      - bench/results/**
      - compat/results/**
      - README.md
      - docs/**
      - .github/workflows/site.yml
  release:
    types: [published]
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: pip install --quiet markdown
      - name: Fetch the released viv, so the command reference matches the release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag=$(gh release view --json tagName --jq .tagName)
          gh release download "$tag" -p "vivace-$tag-x86_64-unknown-linux-musl.tar.gz" -O - | tar xz
          echo "VIV=$PWD/vivace-$tag-x86_64-unknown-linux-musl/viv" >> "$GITHUB_ENV"
      - run: python3 site/build.py
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: site/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
