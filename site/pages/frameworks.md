# For your framework

Install times, plugin handling, the local/Dockerfile/CI snippets and the
compatibility sweep result for the framework starter you use, each read
from the same sources as the rest of the site.

{{framework_list}}
